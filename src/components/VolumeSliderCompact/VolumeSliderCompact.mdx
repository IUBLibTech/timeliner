---
name: Volume Slider Compact
menu: Components
---

import { Props } from 'docz';
import VolumeSliderCompact from './VolumeSliderCompact';
import { VolumeSliderCompactDemo, VolumeSliderCompactFlippedDemo } from './VolumeSliderCompact.demo';
import DemoWrapper from '../docs/components/DemoWrapper/DemoWrapper';

# Volume Slider Compact component
This is the compact variation of the volume slider that takes up less room on the bar.

<Props of={VolumeSliderCompact}/>

## Volume Slider Compact Simple Demo

VolumeSliderDemo component passes the volume to the child components and provides a callback to set the volume.
<DemoWrapper>
  <VolumeSliderCompactDemo />
</DemoWrapper>

## Volume Slider Compact Flipped Demo
Example of the icon and slider flipped around.

<DemoWrapper>
  <VolumeSliderCompactFlippedDemo />
</DemoWrapper>
